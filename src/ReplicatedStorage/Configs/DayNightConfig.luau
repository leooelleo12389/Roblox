local DayNightConfig = {
	-- ระยะเวลา 1 วันเต็ม 24 ชั่วโมงในเกม (ปรับเป็น 1200 วินาที หรือ 20 นาทีจริง เพื่อให้ได้สัดส่วน 12 ชม. ต่อ 10 นาทีจริง)
	CYCLE_DURATION = 1200, 
	
	-- ช่วงเวลาเปลี่ยนผ่าน (หน่วยเป็นชั่วโมงในเกม 0.0 - 24.0)
	DAY_START_HOUR = 6.0,   -- เริ่มสว่าง (6 โมงเช้า)
	NIGHT_START_HOUR = 18.0, -- เริ่มมืด (6 โมงเย็น)
	
	-- บัฟของซอมบี้ตอนกลางคืน
	ZOMBIE_NIGHT_BUFFS = {
		SPEED_MULTIPLIER = 1.4,  -- วิ่งเร็วขึ้น 40%
		DAMAGE_MULTIPLIER = 1.3, -- โจมตีแรงขึ้น 30%
		LOOT_DROP_BUFF = 1.5     -- ซอมบี้ตายแล้วสุ่มดร็อปของดีขึ้น 50%
	}
}

return DayNightConfig
